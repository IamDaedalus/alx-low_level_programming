#include "main.h"

/**
  * mul - multiplies any two given numbers together
  * @m: number one
  * @n: number two
  * Return: returns the result of multiplication
  */
int mul(int m, int n)
{
	return (m * n);
}
